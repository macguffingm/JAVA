package javalec;
// 
public class Lecture_01_01 {
// public -> 접근 지정자 : class 앞에 올 수 있고, 메소드 앞에, 필드 앞에 올 수 있음
//		 	접근지정자는 1) public, 2) protected, 3) 없음(default = package) , 4) private 등 가능
// class -> 사용지정자 class 앞에는 public이 와야 함
	public static void main(String[] args) {
		// static 은 메모리 상에서 정적이라는 의미(하드웨어 적). 써주지 않아도 상관 없음.
		//        void는 반환되는 자료의 타입. void일 경우 반환하지 않겠다는 의미
		//             main은 메소드명. 반드시 소문자로 시작할 것. 괄호가 반드시 있어야 함. 필요한 기능은 중괄호 안에 입력
		// TODO Auto-generated method stub
		System.out.println("Hello World!");
		// 출력하는 기능 println("출력할 내용")\
		// println 메소드는 OS에서만 제어 가능함. 따라서 System 클래스를 이용
		System.out.println("5 * 3 = " + 5*3);
		// sysout이라 치고 ctrl+enter를 치면 자동으로 문구 완성이라는데 나는 안됨
		
		
	}

}
